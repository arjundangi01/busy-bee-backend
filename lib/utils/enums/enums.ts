export enum ENV {
  DEVELOPMENT = "development",
  STAGING = "staging",
  PRODUCTION = "production",
}

export enum AIProviderName {
  OPENROUTER = "openrouter",
  ANTHROPIC = "anthropic",
  OPENAI = "openai",
}
